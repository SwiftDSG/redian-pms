1. Fix dashboard's overview (X)
2. Fix automatic finish (X)
3. Add support for importing tasks from excel (X)
4. Add dashboard task's data (X)
5. Fix parent task's status
6. Add comparison to cumulative reports
